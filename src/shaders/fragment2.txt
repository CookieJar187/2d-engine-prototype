#version 330 core

in vec2 texCoord;

uniform sampler2D spriteTexture;

out vec4 fragmentColor;

void main() {
    fragmentColor = texture(spriteTexture, texCoord);
    if (fragmentColor.a < 0.5f)
        discard;
}